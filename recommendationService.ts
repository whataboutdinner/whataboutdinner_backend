export const getRecipeRecommendations = (userId: string, count: number) => {
  console.log(`Fetching ${count} recipe recommendations for user ${userId}`);
  // Placeholder: Return an empty array or mock data
  return [];
};

export const getRestaurantRecommendations = (userId: string, count: number) => {
  console.log(`Fetching ${count} restaurant recommendations for user ${userId}`);
  // Placeholder: Return an empty array or mock data
  return [];
};

